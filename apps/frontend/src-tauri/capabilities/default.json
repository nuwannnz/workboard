{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core window permissions plus secure token storage for the auth token store.",
  "windows": ["main"],
  "permissions": ["core:default", "store:default"]
}
