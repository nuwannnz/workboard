{
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "name": "tools",
  "projectType": "library",
  "sourceRoot": "tools",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --root tools"
      }
    }
  },
  "tags": ["scope:tools"]
}
